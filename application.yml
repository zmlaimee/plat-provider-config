
spring: #局部application.yml 可以不要
  application:
      name: lovebuy-plat
server:
  port: 8001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    prefer-ip-address: true